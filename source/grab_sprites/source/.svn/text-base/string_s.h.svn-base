#pragma once

//---------------------------------------------------------
//	Include "string_s.h" not <string.h> in my source files.
//---------------------------------------------------------

#include <string.h>

#ifndef _WINDOWS

#define sprintf_s	sprintf
#define strcpy_s	strcpy
#define strcat_s	strcat

extern	char*	strcpy(char* psDest,long DestLength,char* psSource);
extern	char*	strcat(char* psDest,long DestLength,char* psSource);

#endif

